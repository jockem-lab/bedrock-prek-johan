@if($data && $data['class'])
  @if($data['class']['inner'])
    </div>
  @endif
  @if($data['class']['container'])
    </div>
  @endif
  @if($data['class']['wrapper'])
    </div>
  @endif
  @if($data['class']['fluid'])
    </div>
  @endif
@endif